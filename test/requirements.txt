# Cliente HTTP assíncrono para testes
httpx==0.27.0

# Suporte a testes assíncronos e utilitários
pytest==7.4.4
pytest-asyncio==0.23.4

# Pydantic para validação (se necessário expandir o script)
pydantic==2.6.1