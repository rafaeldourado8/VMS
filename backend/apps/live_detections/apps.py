from django.apps import AppConfig

class LiveDetectionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.live_detections'
    verbose_name = 'Detecções ao Vivo'
