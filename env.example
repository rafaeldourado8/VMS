# ======================================================
# === 1. CONFIGURAÇÃO DO DJANGO (BACKEND)
# ======================================================
# Chave secreta do Django - GERE UMA NOVA em produção!
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Modo de debug - SEMPRE False em produção
DEBUG=True

# Hosts permitidos (separe por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1,backend,nginx

# ======================================================
# === 2. BANCO DE DADOS (POSTGRES)
# ======================================================
# Nome do banco de dados
POSTGRES_DB=gtvision_db

# Usuário do banco de dados
POSTGRES_USER=gtvision_user

# Senha do banco de dados - ALTERE para uma senha forte!
POSTGRES_PASSWORD=your-strong-password-here

# Host do banco de dados (use 'localhost' se não estiver usando Docker)
DB_HOST=postgres_db

# Porta do banco de dados
DB_PORT=5432

# ======================================================
# === 3. CACHE (REDIS)
# ======================================================
# Host do Redis (use 'localhost' se não estiver usando Docker)
REDIS_HOST=redis_cache

# Porta do Redis
REDIS_PORT=6379

# URL completa do Redis
REDIS_URL=redis://redis_cache:6379/0

# ======================================================
# === 4. MESSAGE BROKER (RABBITMQ)
# ======================================================
# Usuário do RabbitMQ
RABBITMQ_USER=gtvision_user

# Senha do RabbitMQ - ALTERE para uma senha forte!
RABBITMQ_PASS=your-rabbitmq-password-here

# ======================================================
# === 5. SERVIÇOS E CHAVES
# ======================================================
# API Key para ingestão de dados - GERE uma chave forte!
INGEST_API_KEY=your-ingest-api-key-here

# URL da API do MediaMTX
MEDIAMTX_API_URL=http://mediamtx:9997

# Senha da API do MediaMTX - ALTERE para uma senha forte!
MEDIAMTX_API_PASS=your-mediamtx-password-here

# ======================================================
# === 6. CONFIGURAÇÃO DO NGINX / CORS / FRONTEND
# ======================================================
# Origens permitidas para CORS (separe por vírgula)
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:5173

# Permitir credenciais no CORS
CORS_ALLOW_CREDENTIALS=True

# ======================================================
# === 7. CONFIGURAÇÕES ADICIONAIS
# ======================================================
# Diretório para arquivos estáticos
STATIC_ROOT=/app/staticfiles

# Diretório para arquivos de mídia
MEDIA_ROOT=/app/media

# URL base da aplicação
BASE_URL=http://localhost
