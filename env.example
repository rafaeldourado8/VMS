# ======================================================
# === 1. CONFIGURAÇÃO DO DJANGO (BACKEND)
# ======================================================
SECRET_KEY=django-insecure-substitua-por-uma-chave-secreta-forte
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend,nginx

# ======================================================
# === 2. BANCO DE DADOS (POSTGRES)
# ======================================================
POSTGRES_DB=gtvision_db
POSTGRES_USER=gtvision_user
POSTGRES_PASSWORD=senha-forte-aqui

DB_HOST=postgres_db
DB_PORT=5432

# ======================================================
# === 3. CACHE (REDIS)
# ======================================================
REDIS_HOST=redis_cache
REDIS_PORT=6379
REDIS_URL=redis://redis_cache:6379/0

# ======================================================
# === 4. MESSAGE BROKER (RABBITMQ)
# ======================================================
RABBITMQ_USER=gtvision_user
RABBITMQ_PASS=gtvision_strong_password_12345

# ======================================================
# === 5. SERVIÇOS E CHAVES
# ======================================================

# Chave de ingestão interna (Usada pela IA para se autenticar, se necessário)
INGEST_API_KEY=chave-api-ingestao

# URL da API do MediaMTX (para o Django controlar os paths)
MEDIAMTX_API_URL=http://mediamtx:8888

# ======================================================
# === 6. CONFIGURAÇÃO DO NGINX / CORS / FRONTEND
# ======================================================
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:5173
CORS_ALLOW_CREDENTIALS=True

# ======================================================
# === 7. CONFIGURAÇÕES ADICIONAIS
# ======================================================
STATIC_ROOT=/app/staticfiles
MEDIA_ROOT=/app/media
BASE_URL=http://localhost